<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender com padrão simples para testes -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File appender para logs de teste (opcional) -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/test.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Níveis de log específicos para testes -->
    <logger name="com.geosegbar" level="DEBUG" />
    <logger name="org.springframework" level="INFO" />
    <logger name="org.springframework.test" level="INFO" />
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />
    <logger name="org.testcontainers" level="INFO" />
    <logger name="com.github.dockerjava" level="WARN" />
    <logger name="org.apache.http" level="WARN" />

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <!-- Descomente para habilitar arquivo de log -->
        <!-- <appender-ref ref="FILE" /> -->
    </root>
</configuration>
